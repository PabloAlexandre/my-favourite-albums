{
  "name": "demo-cypress",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "node-sass": "^4.11.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8",
    "serve": "^11.0.0"
  },
  "scripts": {
    "serve": "nohup serve -s build > /tmp/node.log &",
    "serve:stop": "kill $(ps aux | grep 'serve -s build' | grep -v grep | awk '{print $2}')",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "react-router": "^5.0.0",
    "react-router-dom": "^5.0.0"
  }
}
